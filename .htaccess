# RewriteEngine On to enable URL rewriting
RewriteEngine On

# Redirect URLs with .html to URLs without .html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html [L]
